# Default values for ratings-api PREPROD.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

environment: preprod

image:
  repository: quay.io/swoturkey/ratings-api
  tag: master

###### SECRETS ######
mongodb_uri: "mongodb://sa:sa123@ratings-mongo-mongodb.ratingsapp:27017/ratingsdb"